#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Comparison function for qsort (sorting strings lexicographically)
int cmpstr(const void *a, const void *b) {
    char * const *sa = a;
    char * const *sb = b;
    return strcmp(*sa, *sb);
}

// Generate next lexicographical permutation
int next_permutation(int n, char **s)
{
    // 1. Find the rightmost pair where s[i] < s[i+1]
    int i = n - 2;
    while (i >= 0 && strcmp(s[i], s[i + 1]) >= 0)
        i--;

    // If no such index, this is the last permutation
    if (i < 0)
        return 0;

    // 2. Find j such that s[j] > s[i]
    int j = n - 1;
    while (strcmp(s[j], s[i]) <= 0)
        j--;

    // 3. Swap s[i] and s[j]
    char *temp = s[i];
    s[i] = s[j];
    s[j] = temp;

    // 4. Reverse s[i+1..n-1]
    int left = i + 1, right = n - 1;
    while (left < right) {
        temp = s[left];
        s[left] = s[right];
        s[right] = temp;
        left++;
        right--;
    }

    return 1;
}

int main()
{
	char **s;
	int n;
	scanf("%d", &n);
	s = calloc(n, sizeof(char*));
	for (int i = 0; i < n; i++)
	{
		s[i] = calloc(11, sizeof(char));
		scanf("%s", s[i]);
	}
	do
	{
		for (int i = 0; i < n; i++)
			printf("%s%c", s[i], i == n - 1 ? '\n' : ' ');
	} while (next_permutation(n, s));
	for (int i = 0; i < n; i++)
		free(s[i]);
	free(s);
	return 0;
}
